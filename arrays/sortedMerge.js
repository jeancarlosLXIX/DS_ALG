function mergeSortedArr(arr1,arr2){
    console.log([...arr1, ...arr2].sort((a, b) => a - b))
}

value1 = [0,3,4,31]
value2 = [4,6,30]

function mergeSortedArr2(arr1,arr2){
    arrays = [...arr1, ...arr2]
    totalElements = arrays.length - 1
    aux = 0
    
    for (let i = 0; i <= totalElements; i++) {

        
    }
}

mergeSortedArr(value1,value2)